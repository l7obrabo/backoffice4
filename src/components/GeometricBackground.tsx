
import React from "react";

const GeometricBackground: React.FC = () => {
  // Esta é apenas a estrutura para o background
  // O visual real será fornecido pela imagem de fundo via CSS
  
  return <div className="login-background absolute inset-0 -z-10"></div>;
};

export default GeometricBackground;
